<!DOCTYPE html>
<html>
<head>
  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <!-- Custom CSS -->
  <style>
    .welcome-card, .alert {
      animation: fadeIn 0.5s ease-in;
    }
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    .card-body {
      padding: 2.5rem; /* Tighter padding */
    }
    .bi {
      font-size: 1.5rem; /* Larger icons */
    }
    .lead {
      font-size: 1.25rem; /* Smaller lead text */
    }
    .feature-list li {
      font-size: 1rem;
      margin-bottom: 0.5rem;
    }
  </style>
</head>
<body>
  <div class="container my-5"> <!-- Reduced margin -->
    <h1 class="display-3 text-center mb-4">SQL Chatbot</h1> <!-- Tightened title -->

    <!-- Flash Messages -->
    <% if flash[:error] || flash[:alert] %>
      <div class="row justify-content-center">
        <div class="col-lg-10">
          <div class="alert alert-danger alert-dismissible fade show shadow-sm" role="alert">
            <%= flash[:error] || flash[:alert] %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        </div>
      </div>
    <% end %>
    <% if flash[:notice] %>
      <div class="row justify-content-center">
        <div class="col-lg-10">
          <div class="alert alert-success alert-dismissible fade show shadow-sm" role="alert">
            <%= flash[:notice] %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        </div>
      </div>
    <% end %>

    <!-- Welcome and About Section -->
    <div class="row justify-content-center">
      <div class="col-lg-10">
        <div class="card shadow-lg rounded-4 border-0 welcome-card">
          <div class="card-body text-center p-4">
            <% if @sql_credential.present? %>
              <h2 class="h3 mb-3 text-success">
                <i class="bi bi-database-check me-2"></i>Welcome Back <%= current_user.email %>
              </h2>
              <p class="lead mb-4 fs-4">Your database is connected.</p>
            <% else %>
              <h2 class="h3 mb-3 text-primary">
                <i class="bi bi-database-add me-2"></i>Get Started!
              </h2>
              <p class="lead mb-4 fs-4">Connect your database now.</p>
            <% end %>

            <p class="fs-4 mb-3">SQL Chatbot lets you query databases with natural language.</p>
            <ul class="feature-list list-unstyled text-start d-inline-block">
              <li><i class="bi bi-check-circle me-2 text-success"></i>AI-generated SQL queries</li>
              <li><i class="bi bi-check-circle me-2 text-success"></i>Secure credential storage</li>
              <li><i class="bi bi-check-circle me-2 text-success"></i>View chat history</li>
              <li><i class="bi bi-check-circle me-2 text-success"></i>Explore database structure</li>
            </ul>

            <div class="d-grid gap-3 d-md-flex justify-content-center mt-4">
              <% if @sql_credential.present? %>
                <%= link_to sql_credential_path(@sql_credential), class: "btn btn-outline-primary btn-lg px-4" do %>
                  <i class="bi bi-eye-fill me-2"></i>View Credential
                <% end %>
                <%= link_to edit_sql_credential_path(@sql_credential), class: "btn btn-outline-warning btn-lg px-4" do %>
                  <i class="bi bi-pencil-square me-2"></i>Edit Credential
                <% end %>
                <%= link_to chathistory_index_path, class: "btn btn-success btn-lg px-4" do %>
                  <i class="bi bi-chat-dots me-2"></i>Start Querying
                <% end %>
              <% else %>
                <%= link_to new_sql_credential_path, class: "btn btn-success btn-lg px-4" do %>
                  <i class="bi bi-plus-circle me-2"></i>Create Credential
                <% end %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap 5 JS (with Popper.js) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>