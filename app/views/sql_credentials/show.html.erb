<!DOCTYPE html>
<html>
<head>
  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <!-- Custom CSS -->
  <style>
    .credential-card, .tables-card, .alert {
      animation: fadeIn 0.5s ease-in;
    }
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    .list-group-item {
      font-size: 1.1rem; /* Larger text for columns */
      padding: 0.75rem 1.25rem;
    }
    .badge {
      font-size: 1rem; /* Larger badges */
      padding: 0.5em 0.75em;
    }
    .card-body {
      padding: 2rem; /* Spacious padding */
    }
    .table-divider {
      border-bottom: 2px solid #e9ecef; /* Divider between tables */
      padding-bottom: 1.5rem;
    }
  </style>
</head>
<body>
  <div class="container my-6"> <!-- Larger margin -->
    <h1 class="display-3 text-center mb-5">Database Dashboard</h1> <!-- Larger, friendly title -->

    <!-- SQL Credential Section -->
    <% if @sql_credential %>
      <div class="row justify-content-center">
        <div class="col-lg-10"> <!-- Wider column -->
          <div class="card shadow-sm rounded-4 border-0 mb-5 credential-card">
            <div class="card-header bg-primary text-white text-center">
              <h2 class="h3 mb-0"><i class="bi bi-database-fill-gear me-2"></i>Your SQL Credential</h2>
            </div>
            <div class="card-body">
              <div class="row">
                <div class="col-md-4 mb-3">
                  <p class="fs-5"><strong>Host:</strong> <span class="badge bg-secondary"><%= @sql_credential.host %></span></p>
                </div>
                <div class="col-md-4 mb-3">
                  <p class="fs-5"><strong>Username:</strong> <span class="badge bg-secondary"><%= @sql_credential.username %></span></p>
                </div>
                <div class="col-md-4 mb-3">
                  <p class="fs-5"><strong>Database:</strong> <span class="badge bg-secondary"><%= @sql_credential.database %></span></p>
                </div>
              </div>
              <div class="d-flex gap-2 mt-4">
                <%= link_to home_index_path, class: "btn btn-success btn-lg" do %>
                  <i class="bi bi-arrow-left me-2"></i>Back
                <% end %>
                <%= link_to edit_sql_credential_path(@sql_credential), class: "btn btn-warning btn-lg" do %>
                  <i class="bi bi-pencil-square me-2"></i>Edit SQL Credential
                <% end %>
                <%= link_to chathistory_index_path, class: "btn btn-success btn-lg" do %>
                  <i class="bi bi-chat-dots me-2"></i>Chat with Database
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% else %>
      <div class="row justify-content-center">
        <div class="col-lg-10">
          <div class="alert alert-info alert-dismissible fade show text-center" role="alert">
            <h4 class="alert-heading">No SQL Credential Found</h4>
            <p>Please add your database details to continue.</p>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        </div>
      </div>
    <% end %>

    <!-- Database Structure Section -->
    <% if @tables_info.present? %>
      <div class="row justify-content-center">
        <div class="col-lg-10">
          <div class="card shadow-sm rounded-4 border-0 tables-card">
            <div class="card-header bg-info text-white text-center">
              <h3 class="h3 mb-0"><i class="bi bi-table me-2"></i>Database Structure</h3>
            </div>
            <div class="card-body">
              <% @current_page_tables.each do |table| %>
                <div class="mb-4 table-divider">
                  <h5 class="text-primary fs-4"><i class="bi bi-table me-2"></i>Table: <strong><%= table[:table_name] %></strong></h5>
                  <ul class="list-group list-group-flush">
                    <% table[:columns].each do |column| %>
                      <li class="list-group-item"><i class="bi bi-arrow-right-short me-2"></i><%= column %></li>
                    <% end %>
                  </ul>
                </div>
              <% end %>
              <!-- Pagination -->
              <div class="d-flex justify-content-center mt-4">
                <%= paginate @tables_info, previous_label: 'Prev', next_label: 'Next', outer_window: 0, inner_window: 2, class: "pagination pagination-lg" %>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% else %>
      <div class="row justify-content-center">
        <div class="col-lg-10">
          <div class="alert alert-warning alert-dismissible fade show text-center mt-4" role="alert">
            <h5 class="alert-heading">No Tables Available</h5>
            <p>Your database does not have any tables yet. Please create tables to start using the database.</p>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <!-- Bootstrap 5 JS (with Popper.js) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>