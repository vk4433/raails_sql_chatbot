<!DOCTYPE html>
<html>
<head>
  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <!-- Custom CSS -->
  <style>
    .edit-card, .alert {
      animation: fadeIn 0.5s ease-in;
    }
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    .card-body {
      padding: 3rem; /* Spacious padding */
    }
    .bi {
      font-size: 1.5rem; /* Larger icons */
    }
    .alert {
      font-size: 1.1rem; /* Larger error text */
    }
  </style>
</head>
<body>
  <div class="container my-6"> <!-- Larger margin -->
    <h1 class="display-3 text-center mb-5">Edit SQL Credential</h1> <!-- Larger, bold title -->

    <div class="row justify-content-center">
      <div class="col-lg-10"> <!-- Wider column -->
        <div class="card shadow-lg rounded-4 border-0 edit-card">
          <div class="card-header bg-info text-white text-center">
            <h3 class="mb-0"><i class="bi bi-pencil-square me-2"></i>Update Your SQL Credential</h3>
          </div>
          <div class="card-body p-5">
            <!-- Error Messages -->
            <% if @sql_credential.errors.any? %>
              <div class="alert alert-danger alert-dismissible fade show shadow-sm" role="alert">
                <h5 class="fs-5"><%= pluralize(@sql_credential.errors.count, "error") %> prohibited this SQL Credential from being saved:</h5>
                <ul class="mb-0">
                  <% @sql_credential.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
              </div>
            <% end %>

            <!-- Form -->
            <%= form_with(model: @sql_credential, local: true) do |form| %>
              <div class="mb-4">
                <%= form.label :host, class: "form-label fw-bold fs-4" %>
                <%= form.text_field :host, class: "form-control form-control-lg", placeholder: "e.g., localhost" %>
              </div>

              <div class="mb-4">
                <%= form.label :username, class: "form-label fw-bold fs-4" %>
                <%= form.text_field :username, class: "form-control form-control-lg", placeholder: "e.g., root" %>
              </div>

              <div class="mb-4">
                <%= form.label :password, class: "form-label fw-bold fs-4" %>
                <%= form.password_field :password, class: "form-control form-control-lg", placeholder: "Your DB password" %>
              </div>

              <div class="mb-4">
                <%= form.label :database, class: "form-label fw-bold fs-4" %>
                <%= form.text_field :database, class: "form-control form-control-lg", placeholder: "e.g., my_database" %>
              </div>

              <div class="mb-4">
                <%= form.label :port, class: "form-label fw-bold fs-4" %>
                <%= form.number_field :port, class: "form-control form-control-lg", placeholder: "e.g., 3306" %>
              </div>

              <div class="mb-4">
                <%= form.label :socket, class: "form-label fw-bold fs-4" %>
                <%= form.text_field :socket, class: "form-control form-control-lg", placeholder: "e.g., /var/run/mysqld/mysqld.sock" %>
              </div>

              <div class="d-flex justify-content-end mt-4">
                <%= form.submit "Update SQL Credential", class: "btn btn-info btn-lg px-5 text-white" do %>
                  <i class="bi bi-save me-2"></i>Update SQL Credential
                <% end %>
              </div>
            <% end %>

            <div class="d-flex justify-content-start mt-3">
              <%= link_to sql_credential_path(@sql_credential), class: "btn btn-outline-secondary btn-lg px-5" do %>
                <i class="bi bi-arrow-left me-2"></i>Back
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap 5 JS (with Popper.js) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>